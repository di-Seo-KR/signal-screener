# DI금융 v9.7 일일 개선 로그 — 2026-03-18

## 변경 사항

### 1. 다중 타임프레임 RSI 확인 (Multi-Timeframe RSI)
- `analyzeAsset()`에 일간 RSI(14) 계산 추가 (`dailyRsi`)
- `quickDiagnosis()`에 주간+일간 RSI 동시 과매도/과매수 감지 로직 추가
  - 주간 RSI ≤30 AND 일간 RSI ≤30 → momScore +6, 시그널 "MTF RSI 동시 과매도"
  - 주간 RSI ≥70 AND 일간 RSI ≥70 → momScore -6, 시그널 "MTF RSI 동시 과매수"
- AssetCard 지표 상세 그리드에 "RSI(14) 일간" 칼럼 추가 (MTF 확인 sub label 포함)

### 2. findSwings 리팩토링
- MACD 다이버전스와 RSI 다이버전스에서 중복된 `findSwings` / `findSwingsRsi` 함수를 공유 헬퍼 `findSwingPoints()`로 통합
- 코드 14줄 절감, 유지보수성 향상

### 3. UI/UX 개선
- AssetCard에 hover 효과 추가: `border-color` 전환(blue), `box-shadow` 글로우
- 모바일 터치 최적화: 차트 보기 / 상세 정보 버튼에 `minHeight: 44px`, 패딩 확대
- 버튼 flex-wrap 추가로 작은 화면에서 줄바꿈 대응
- 상세 정보 링크에 `display: inline-flex` + `alignItems: center` 추가

### 4. 버전 업데이트
- v9.1 → v9.7 (사이드바, 주석)

## 빌드 검증
- `vite build` 성공 (44 modules, 858KB gzip 259KB)

## 기존 개선사항 확인 (이미 반영됨)
- CMF/MFI 값 스크리너 UI 표시 ✅
- macdDivType 표시 ✅
- BB 스퀴즈 Keltner Channel ✅
- 전략 32개 정상 운용 ✅

## 다음 검토 과제 (P3)
- 다중 타임프레임 확장: 월간 RSI 추가 가능성
- 코드 스플리팅 (동적 import로 번들 크기 최적화)
- 백테스트 하위 전략 교체 검토 (성과 데이터 수집 후)
