# Zepta 에이전트 워크플로우 가이드

> 이 문서는 실제 에이전트 투입 시 참조하는 **실행 가이드**입니다.
> 조직 구조와 역할 정의는 `TEAM_STRUCTURE.md`를 참조하세요.
> 프로젝트 설정과 행동 규칙은 `CLAUDE.md`를 참조하세요.

---

## 에이전트 투입 템플릿

### Template A: 풀 팀 투입 (대규모 작업)

**Phase 1 — 전략 수립 (3명 병렬)**
```
Agent 1 (PLAN-BIZ):
  프롬프트: "Zepta 프로젝트의 사업 기획자입니다. [작업 내용] 에 대해 
  사업 관점에서 분석해주세요. 유입 확대, 수익 창출, 경쟁사 대비 
  차별화 관점에서 전략을 제시해주세요. 코드는 작성하지 마세요."

Agent 2 (PLAN-SVC):
  프롬프트: "Zepta 프로젝트의 서비스 기획자입니다. [작업 내용] 에 대해 
  서비스 스펙을 작성해주세요. 화면 구성, UX 플로우, 정보 구조, 
  컴포넌트 목록을 포함해주세요. 코드는 작성하지 마세요."

Agent 3 (MKT-LEAD):
  프롬프트: "Zepta 프로젝트의 시니어 마케터입니다. [작업 내용] 에 대해 
  SEO, 유입, 광고 수익, 리텐션 관점에서 요구사항을 제시해주세요. 
  즉시 실행 가능한 액션 플랜도 포함해주세요. 코드는 작성하지 마세요."
```

**Phase 2 — 아키텍처 설계 (1명)**
```
Agent 4 (DEV-ARCH):
  프롬프트: "시니어 시스템 아키텍트입니다. 다음 통합 스펙에 따라 
  아키텍처를 설계해주세요: [Phase 1 통합 결과]. 
  파일별 변경 범위, 경계 사례, 리스크를 분석하고 구현 계획을 
  수립해주세요. 코드는 작성하지 마세요."
```

**Phase 3 — 구현 + QA + 성능 (3명 병렬)**
```
Agent 5 (DEV-IMPL):
  프롬프트: "시니어 프론트엔드 엔지니어입니다. 다음 아키텍처 설계에 따라 
  코드를 구현해주세요: [DEV-ARCH 설계 결과]. 
  프로덕션 수준, 접근성, 반응형, i18n을 모두 고려해주세요."

Agent 6 (DEV-QA):
  프롬프트: "시니어 QA 엔지니어입니다. [DEV-IMPL의 코드]를 리뷰해주세요. 
  버그, 경계 사례, 보안 취약점, 접근성, 반응형 호환성을 검증하고 
  수정이 필요한 부분을 구체적으로 지적해주세요."

Agent 7 (DEV-PERF):
  프롬프트: "퍼포먼스 엔지니어입니다. [구현된 코드]의 성능을 분석해주세요. 
  불필요한 리렌더링, 번들 사이즈 영향, 메모리 누수 가능성을 
  점검하고 최적화 방안을 제시해주세요."
```

---

### Template B: 개발팀만 투입 (중규모 작업)

```
Agent 1 (DEV-ARCH): 빠른 구조 분석 + 구현 계획
Agent 2 (DEV-IMPL): 코드 구현
Agent 3 (DEV-QA): 품질 리뷰
(DEV-PERF는 성능 이슈가 예상될 때만 투입)
```

---

### Template C: UI/UX 최적화 (디자인 중심)

```
Agent 1 (PLAN-SVC): UX 개선안 + 화면 스펙
Agent 2 (DEV-IMPL #1): 좌측 영역 UI 구현
Agent 3 (DEV-IMPL #2): 우측 영역 UI 구현  
Agent 4 (DEV-IMPL #3): 애니메이션 + 인터랙션
(필요 시 DEV-QA, DEV-PERF 추가 투입)
```

---

## 에이전트 공통 규칙

1. **한국어 존댓말** — 모든 출력, 주석, 커밋 메시지
2. **처음부터 제대로** — 반복 수정 최소화
3. **기획/마케팅 에이전트는 코드 작성 금지** — 분석과 스펙만
4. **개발 에이전트만 코드 수정** — 메인 Claude가 최종 검증
5. **병렬 투입 극대화** — 독립적 작업은 반드시 동시 진행
6. **결과물은 구체적으로** — 추상적 제안 금지, 실행 가능한 수준으로

---

## 예시 시나리오

### 시나리오 1: "새로운 프리미엄 기능 추가"
```
Phase 1 (병렬):
  PLAN-BIZ → 프리미엄 수익 모델 설계 (가격, 티어, 경쟁사 비교)
  PLAN-SVC → 프리미엄 화면 UX 설계 (결제 플로우, 기능 잠금 UI)
  MKT-LEAD → 프리미엄 전환율 최적화 전략 (트라이얼, 업셀, SEO)

Phase 2:
  DEV-ARCH → 결제 시스템 아키텍처, Supabase 스키마 설계

Phase 3 (병렬):
  DEV-IMPL → 프리미엄 UI + 결제 연동 구현
  DEV-QA → 결제 플로우 전체 테스트, 보안 검증
  DEV-PERF → 결제 관련 API 응답 시간 최적화
```

### 시나리오 2: "홈 화면 UI 최적화"
```
Phase 1 (병렬):
  PLAN-SVC → 홈 화면 UX 개선안 (레이아웃, 정보 계층)
  MKT-LEAD → SEO 관점 홈 화면 요구사항

Phase 2 (병렬):
  DEV-IMPL #1 → 메인 콘텐츠 영역 UI 개선
  DEV-IMPL #2 → 사이드바 영역 UI 개선
  DEV-IMPL #3 → 애니메이션 + 호버 효과

Phase 3:
  DEV-QA → 반응형 + 접근성 검증
  DEV-PERF → 렌더링 성능 확인
```

### 시나리오 3: "긴급 버그 수정"
```
즉시 처리 (에이전트 투입 없음):
  메인 Claude → 버그 분석 → 수정 → 커밋 → push 요청
```

---

---

## 일일 자동 스탠드업 (Daily Standup) — 2026-05 도입

### 개요
매일 KST 06:00 에 Vercel cron 이 `/api/agents/daily-standup` 을 호출하여
지정된 에이전트들이 Claude API 를 통해 분석 → 텔레그램으로 일괄 발송.

### 운영 모드
- **자동 실행**: vercel.json `crons` 의 `"path": "/api/agents/daily-standup"` 항목 참고
- **수동 실행 (dry run)**: `GET /api/agents/daily-standup?dryRun=1` — 텔레그램 발송 없이 JSON 응답으로 결과 확인
- **수동 실행 (실 발송)**: `GET /api/agents/daily-standup`

### Week 1 — 퀀트팀 (현재 가동)

| 코드명 | 역할 | 입력 | 출력 |
|---|---|---|---|
| QUANT-RES | 알파 리서처 | shadow ledger 7일치 + 가중치 | 헤드라인 / 어제 잘된 것·부진한 것 / 오늘 주목 / 새 알파 후보 / 행동 가이드 |
| QUANT-PLAN | 전략 기획자 | 동일 입력 + RES 메타 | 헤드라인 / 승급·디머지 후보 / 가중치 조정 / 리스크 경고 |

### 텔레그램 메시지 표준
- 모든 에이전트 메시지는 `api/_shared/telegram.js` 의 `buildCard()` + `sendCards()` 사용
- **금기**: 전문 용어 직역 (예: "Hurst 레짐 변화 0.45→0.62") — 평어로 풀어서 비유까지
- **권장**: `tag` (이모지) → `title` (한 줄 제목) → `lines[]` (불릿) → `hint` (그래서 뭐) → `footer` (출처)

### Week 2~ 확장 계획

```
Week 2: MKT-LEAD
  - 입력: GA4 어제 데이터, 검색어 트렌드, 경쟁사 변화
  - 출력: 어제 유입 요약, 오늘 콘텐츠 1건 초안, 광고 최적화 1건

Week 3: PLAN-SVC + PLAN-BIZ
  - 입력: Supabase 사용자 행동 로그, GA 퍼널, Sentry 이슈
  - 출력: PLAN-SVC → 화면 개선 제안 1건
        PLAN-BIZ → 경쟁사 변화 + 수익 다각화 제안

Week 4: DEV-IMPL + DEV-PERF
  - 입력: Sentry 새 이슈, Lighthouse 어제 대비 변화, npm audit
  - 출력: DEV-IMPL → 픽스 후보 PR 초안 (위험도 분류)
        DEV-PERF → 회귀 알림 / 청크 사이즈 변화
```

### 비용 / 한도
- Claude API 토큰: 역할당 ~3,000 input + ~1,500 output = $0.05 ~ $0.10/일
- 7개 역할 매일 = 월 약 $10 ~ $20 (Sonnet 4.6 기준)
- Vercel cron: Hobby 플랜 무료 한도 내

### 코드 위치
- `api/agents/daily-standup.js` — 메인 핸들러
- `api/_shared/telegram.js` — 공용 텔레그램 발송
- `vercel.json` `crons` — 스케줄 등록

---

**마지막 업데이트**: 2026-05-01
**적용 대상**: Zepta 팀 전체
